import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import Link from "next/link";
import { cookies } from "next/headers";
import { verifyAuthToken } from "@/lib/auth";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const cookieStore = await cookies();
  const token = cookieStore.get(process.env.JWT_COOKIE_NAME || "blog_token")?.value || null;
  const payload = token ? verifyAuthToken(token) : null;

  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <header className="border-b border-gray-200 bg-white/80 backdrop-blur">
          <nav className="mx-auto flex max-w-5xl items-center justify-between p-4">
            <div className="flex items-center gap-4">
              <Link className="font-semibold" href="/">Anasayfa</Link>
              {payload?.role === 'admin' && (
                <Link className="text-gray-700 hover:underline" href="/admin">Admin</Link>
              )}
            </div>
            <div className="flex items-center gap-3">
              {!payload ? (
                <>
                  <Link className="text-gray-700 hover:underline" href="/login">Giriş</Link>
                  <Link className="rounded-md bg-indigo-600 px-3 py-1.5 text-white hover:bg-indigo-500" href="/signup">Kayıt ol</Link>
                </>
              ) : (
                <form action="/api/logout" method="post">
                  <button className="rounded-md bg-gray-800 px-3 py-1.5 text-white hover:bg-gray-700" type="submit">Çıkış</button>
                </form>
              )}
            </div>
          </nav>
        </header>
        <main>{children}</main>
      </body>
    </html>
  );
}
